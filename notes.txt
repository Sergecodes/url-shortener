source /home/sergeman/.virtualenvs/urlshortener-env/bin/activate 


whitenoise==
django-debug-toolbar==


TODO:
   BACKEND:
      - link django hosts and enable admin. url ...
      - include redis in implementation(save both in db and cache, when retrieving, first check in cache. if not found, then check db.) create util functions...
      - use whitenoise with cloudfront
      - write tests
   
   FRONTEND:
      - content of result modal (index page)
      - work on preview page
      - cookie disclaimer
      - work on 404, 403, 500 pages


TO INSTALL:
- install nodejs
- setup django-tailwind (https://django-tailwind.readthedocs.io/en/latest/installation.html)
- setup geoip2 (https://docs.djangoproject.com/en/3.2/ref/contrib/gis/geoip2)
